import styled from "styled-components";

export const StyledSlickSliderWrapper = styled.div`
  max-width: 1200px;
  min-height: 600px;
  margin: 0 auto;
  .slick-arrow::before {
    color: #444;
  }
`;
