import styled from "styled-components";
import { motion } from "framer-motion";

export const StyledInnerCarousel = styled(motion.div)`
  display: flex;
  align-items: center;
  padding: 40px 0;
  /* background-color: lightblue; */
`;
